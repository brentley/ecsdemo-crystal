---
service:
  minSize: 3
  desiredCount: 3
  maxSize: 6
  port: 3000
  discoveryTTL: 5
  pipeline:
      acceptance:
        environment: crystal
      production:
        disabled: true   
  schedules:
  - name: minutely
    expression: cron(* * * * ? *)
    command: ['echo','It looks like the date is $(date)']

  database:
      name: crystaldb  
      allocatedStorage: 100
